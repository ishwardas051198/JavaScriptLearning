// import {order, item } from './product.js'

// order("ishwar","chicken")
// console.log(item)


//recent change on await that we cna write await to the top level function 

//example

let data=await fetch("product.js");
console.log(data);

//actually top level await will stop the execution of next parts


//closures
//means inner function has an access to the variables and parameters of outer function
//it points out to the reference of the variable not assigning the variable again and again


